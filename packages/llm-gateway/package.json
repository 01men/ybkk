{
  "name": "@aios/llm-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "元冰可可 AIOS — LLM 抽象网关（Qwen-Local / OpenAI / Anthropic）",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run --coverage",
    "lint": "eslint src tests",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.13",
    "typescript": "^5.5.4",
    "vitest": "^2.1.5"
  }
}